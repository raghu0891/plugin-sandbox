exec plugin bridges list --help
cmp stdout out.txt

-- out.txt --
NAME:
   plugin bridges list - List all Bridges to External Adapters

USAGE:
   plugin bridges list [command options] [arguments...]

OPTIONS:
   --page value  page of results to display (default: 0)
   
