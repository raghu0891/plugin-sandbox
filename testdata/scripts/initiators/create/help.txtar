exec plugin initiators create --help
cmp stdout out.txt

-- out.txt --
NAME:
   plugin initiators create - Create an authentication key for a user of External Initiators

USAGE:
   plugin initiators create [arguments...]
