exec plugin keys dkgsign --help
cmp stdout out.txt

-- out.txt --
NAME:
   plugin keys dkgsign - Remote commands for administering the node's DKGSign keys

USAGE:
   plugin keys dkgsign command [command options] [arguments...]

COMMANDS:
   create  Create a DKGSign key
   import  Import DKGSign key from keyfile
   export  Export DKGSign key to keyfile
   delete  Delete DKGSign key if present
   list    List the DKGSign keys

OPTIONS:
   --help, -h  show help
   
