exec plugin health --help
cmp stdout out.txt

-- out.txt --
NAME:
   plugin health - Prints a health report

USAGE:
   plugin health [command options] [arguments...]

OPTIONS:
   --failing, -f  filter for failing services
   --json, -j     json output
   
