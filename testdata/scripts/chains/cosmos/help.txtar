exec plugin chains cosmos --help
cmp stdout out.txt

-- out.txt --
NAME:
   plugin chains cosmos - Commands for handling Cosmos chains

USAGE:
   plugin chains cosmos command [command options] [arguments...]

COMMANDS:
   list  List all existing Cosmos chains

OPTIONS:
   --help, -h  show help
   
