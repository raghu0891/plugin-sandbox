exec plugin chains starknet list --help
cmp stdout out.txt

-- out.txt --
NAME:
   plugin chains starknet list - List all existing StarkNet chains

USAGE:
   plugin chains starknet list [arguments...]
