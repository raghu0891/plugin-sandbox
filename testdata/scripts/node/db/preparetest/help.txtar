exec plugin node db preparetest --help
cmp stdout out.txt
! stderr .

-- out.txt --
NAME:
   plugin node db preparetest - Reset database and load fixtures.

USAGE:
   plugin node db preparetest [command options] [arguments...]

OPTIONS:
   --user-only  only include test user fixture
   
