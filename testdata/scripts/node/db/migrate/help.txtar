exec plugin node db migrate --help
cmp stdout out.txt
! stderr .

-- out.txt --
NAME:
   plugin node db migrate - Migrate the database to the latest version.

USAGE:
   plugin node db migrate [arguments...]
