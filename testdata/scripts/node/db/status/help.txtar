exec plugin node db status --help
cmp stdout out.txt
! stderr .

-- out.txt --
NAME:
   plugin node db status - Display the current database migration status.

USAGE:
   plugin node db status [arguments...]
