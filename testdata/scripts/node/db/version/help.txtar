exec plugin node db version --help
cmp stdout out.txt
! stderr .

-- out.txt --
NAME:
   plugin node db version - Display the current database version.

USAGE:
   plugin node db version [arguments...]
