exec plugin config --help
cmp stdout out.txt

-- out.txt --
NAME:
   plugin config - Commands for the node's configuration

USAGE:
   plugin config command [command options] [arguments...]

COMMANDS:
   show      Show the application configuration
   loglevel  Set log level
   logsql    Enable/disable SQL statement logging

OPTIONS:
   --help, -h  show help
   
