exec plugin txs evm list --help
cmp stdout out.txt

-- out.txt --
NAME:
   plugin txs evm list - List the Ethereum Transactions in descending order

USAGE:
   plugin txs evm list [command options] [arguments...]

OPTIONS:
   --page value  page of results to display (default: 0)
   
