exec plugin jobs list --help
cmp stdout out.txt

-- out.txt --
NAME:
   plugin jobs list - List all jobs

USAGE:
   plugin jobs list [command options] [arguments...]

OPTIONS:
   --page value  page of results to display (default: 0)
   
