exec plugin jobs --help
cmp stdout out.txt

-- out.txt --
NAME:
   plugin jobs - Commands for managing Jobs

USAGE:
   plugin jobs command [command options] [arguments...]

COMMANDS:
   list    List all jobs
   show    Show a job
   create  Create a job
   delete  Delete a job
   run     Trigger a job run

OPTIONS:
   --help, -h  show help
   
